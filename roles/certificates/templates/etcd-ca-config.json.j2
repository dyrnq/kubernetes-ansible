{
  "signing": {
    "profiles": {
      "server": {
        "expiry": "{{ cert_expiry*365*24 }}h",
        "usages": [
          "signing",
          "key encipherment",
          "server auth"
        ]
      },
      "client": {
        "expiry": "{{ cert_expiry*365*24 }}h",
        "usages": [
          "signing",
          "key encipherment",
          "client auth"
        ]
      },
      "peer": {
        "expiry": "{{ cert_expiry*365*24 }}h",
        "usages": [
          "signing",
          "key encipherment",
          "server auth",
          "client auth"
        ]
      }
    }
  }
}