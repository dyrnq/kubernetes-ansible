{
  "signing": {
    "default": {
      "expiry": "{{ certificates.expiry*365*24 }}h"
    },
    "profiles": {
      "client": {
        "expiry": "{{ certificates.expiry*365*24 }}h",
        "usages": [
          "signing",
          "key encipherment",
          "client auth"
        ]
      }
    }
  }
}