{
  "signing": {
    "default": {
      "expiry": "{{ cert_expiry*365*24 }}h"
    },
    "profiles": {
      "server": {
        "expiry": "{{ cert_expiry*365*24 }}h",
        "usages": [
          "signing",
          "key encipherment",
          "server auth"
        ]
      },
      "client": {
        "expiry": "{{ cert_expiry*365*24 }}h",
        "usages": [
          "signing",
          "key encipherment",
          "client auth"
        ]
      }
    }
  }
}